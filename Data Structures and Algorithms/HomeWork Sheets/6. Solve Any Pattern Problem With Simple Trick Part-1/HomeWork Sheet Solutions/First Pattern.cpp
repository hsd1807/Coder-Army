#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);cout.tie(0);
    int row,col;

    for(row=1;row<=5;row++)
    {
        for(col=1;col<=6;col++)
            cout<<4<<" ";
        cout<<endl;
    }
    return 0;
}

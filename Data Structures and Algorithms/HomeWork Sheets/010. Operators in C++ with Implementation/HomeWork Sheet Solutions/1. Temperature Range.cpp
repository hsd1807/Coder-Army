#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);cout.tie(0);
    int temp;
    cin>>temp;

    if(temp>70&&temp<90)
        cout<<"Yes";
    else
        cout<<"No";

    return 0;
}

#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);cout.tie(0);
    int age;
    cin>>age;

    if(age>=13&&age<=19)
        cout<<"Yes";
    else
        cout<<"No";

    return 0;
}
